# Skitch Studio

A static web annotation app inspired by Skitch. It supports screenshot import or paste, editable vector annotations, styled text boxes, stamps, privacy patches, crop, undo/redo, and PNG export.

Live site: https://skitch-studio.tin-kevin.chatgpt.site

Open `index.html` locally or deploy the directory as a static website.
